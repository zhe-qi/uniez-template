# Base URL for API requests (Production Environment)
# This is the root endpoint for all API calls in production
# Ensure this matches your production server configuration
# Critical for proper API communication in live environment
VITE_APP_BASEURL=/
